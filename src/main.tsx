import React,{useMemo,useState}from'react';import{createRoot}from'react-dom/client';import{ArrowRight,CheckCircle2,Globe2,Home,MapPin,Plus,Search,ShieldCheck,Sparkles,WalletCards,X,Menu,MessageCircle,UserRound}from'lucide-react';import{countryConfigs,errandCategories,getCountryConfig}from'./errand-config';import'./styles.css';type Errand={id:number;title:string;category:string;location:string;price:number;status:string};const seed:Errand[]=[{id:1,title:'Pick up groceries and deliver them',category:'Shopping',location:'Wuse, Abuja',price:8500,status:'Open'},{id:2,title:'Clean a 2-bedroom apartment',category:'House Chores',location:'Maitama, Abuja',price:30000,status:'Open'},{id:3,title:'Deliver documents to an office',category:'Run an Errand',location:'Central Area, Abuja',price:7000,status:'Open'},{id:4,title:'Weekly office cleaning',category:'Office Chores',location:'Garki, Abuja',price:45000,status:'Open'}];
function App(){const[country,setCountry]=useState('NG'),[category,setCategory]=useState('all'),[search,setSearch]=useState(''),[post,setPost]=useState(false),[auth,setAuth]=useState<'signup'|'login'|null>(null),[dash,setDash]=useState(false),[errands,setErrands]=useState(seed),[wallet,setWallet]=useState(0),[menu,setMenu]=useState(false);const config=getCountryConfig(country);const filtered=useMemo(()=>errands.filter(e=>(category==='all'||e.category===category)&&`${e.title} ${e.location}`.toLowerCase().includes(search.toLowerCase())),[errands,category,search]);function add(f:FormData){setErrands(x=>[{id:Date.now(),title:String(f.get('title')),category:String(f.get('category')),location:String(f.get('location')),price:Number(f.get('price')),status:'Open'},...x]);setPost(false)}if(dash)return <Dashboard config={config} wallet={wallet} back={()=>setDash(false)} />;return <div className="app"><header><div className="brand"><span className="logo"><Sparkles size={19}/></span><div><b>Errand<span>Go</span></b><small>Get things done. Get paid.</small></div></div><nav className={menu?'open':''}><a href="#services">Services</a><a href="#tasks">Find errands</a><a href="#how">How it works</a><button className="textbtn" onClick={()=>setAuth('login')}>Log in</button><button className="primary small" onClick={()=>setAuth('signup')}>Sign up free</button></nav><button className="menubtn" onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></header><section className="hero"><div className="hero-copy"><div className="eyebrow">● THE EVERYDAY HELP MARKETPLACE</div><h1>Life gets busy.<br/><em>Let someone help.</em></h1><p>ErrandGo connects you with trusted people for errands, home chores, office tasks and local help — while giving workers a simple way to earn.</p><div className="hero-actions"><button className="primary" onClick={()=>setPost(true)}>Post an errand <ArrowRight/></button><button className="secondary" onClick={()=>setAuth('signup')}>Become a tasker</button></div><div className="mini-trust"><CheckCircle2/> Verified profiles · Secure payments · Real reviews</div></div><div className="hero-panel"><div className="panel-main"><span className="panel-icon"><Home/></span><h3>What do you need help with?</h3><p>Find a trusted local person to get it done.</p><button className="searchbox" onClick={()=>document.getElementById('tasks')?.scrollIntoView({behavior:'smooth'})}><Search/> Search errands</button></div><div className="floating-card"><span className="avatar">AM</span><div><b>Errand completed</b><small>Groceries · just now</small></div><strong>+₦8,500</strong></div></div></section><section className="marketbar"><div><Globe2/> Serving people around the world</div><select value={country} onChange={e=>setCountry(e.target.value)}>{countryConfigs.map(c=><option value={c.code} key={c.code}>{c.name} · {c.symbol} {c.currency}</option>)}</select><span>Languages: {config.languages.slice(0,3).join(', ')}</span></section><section className="services" id="services"><div className="section-title"><span>EXPLORE SERVICES</span><h2>Help for the things<br/><em>you don't have time for.</em></h2></div><div className="service-grid">{[['🏠','Home chores','Cleaning, laundry & organizing','House Chores'],['🛒','Shopping','Groceries & everyday purchases','Shopping'],['📦','Delivery','Pickups, drop-offs & documents','Run an Errand'],['🏢','Office help','Workplace tasks & support','Office Chores'],['🚚','Moving & lifting','Packing and local assistance','all'],['✨','Anything else','Post your custom task','all']].map(s=><button key={s[1]} onClick={()=>{setCategory(s[3]);document.getElementById('tasks')?.scrollIntoView({behavior:'smooth'})}}><span className="service-icon">{s[0]}</span><b>{s[1]}</b><small>{s[2]}</small><ArrowRight/></button>)}</div></section><section className="how" id="how"><div><span>01</span><b>Post what you need</b><p>Tell us the task, location, timing and budget.</p></div><div><span>02</span><b>Choose your tasker</b><p>Compare profiles, ratings, experience and offers.</p></div><div><span>03</span><b>Get it done</b><p>Chat, track progress and release payment when complete.</p></div></section><section className="tasks" id="tasks"><div className="section-head"><div><div className="eyebrow">FIND LOCAL WORK</div><h2>Errands near you</h2><p>Earn money by helping people around you.</p></div><div className="search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search tasks or locations"/></div></div><div className="chips"><button className={category==='all'?'active':''} onClick={()=>setCategory('all')}>All</button>{errandCategories.map(c=><button className={category===c.label?'active':''} onClick={()=>setCategory(c.label)} key={c.id}>{c.label}</button>)}</div><div className="grid">{filtered.map(e=><article key={e.id}><div className="card-top"><div><span className="tag">{e.category}</span><h3>{e.title}</h3></div><div className="price">{config.symbol}{e.price.toLocaleString()}<small>budget</small></div></div><div className="meta"><span><MapPin/>{e.location}</span><span><CheckCircle2/>{e.status}</span></div><button className="accept" onClick={()=>setAuth('login')}>Accept task <ArrowRight/></button></article>)}</div></section><section className="earn"><div><div className="eyebrow">FOR TASKERS</div><h2>Your time is valuable.<br/><em>Make it pay.</em></h2><p>Choose the jobs you want, set your schedule and build your reputation through great work.</p><button className="primary" onClick={()=>setAuth('signup')}>Start earning <ArrowRight/></button></div><div className="earn-stats"><div><strong>4.9★</strong><span>Average rating</span></div><div><strong>24/7</strong><span>Tasks posted</span></div><div><strong>100%</strong><span>You choose your work</span></div></div></section><section className="trust-section"><div><ShieldCheck/><h3>Built around trust</h3><p>Verification, ratings, reporting and dispute handling are designed into the platform.</p></div><div><WalletCards/><h3>Local payments</h3><p>Currency and payment options adapt to your country.</p></div><div><MessageCircle/><h3>Chat before you hire</h3><p>Keep task details, questions and updates together.</p></div></section><footer><div className="brand"><span className="logo"><Sparkles size={17}/></span><b>Errand<span>Go</span></b></div><span>Global errands. Local opportunities.</span><span>© 2026 ErrandGo</span></footer>{post&&<div className="modal"><form onSubmit={e=>{e.preventDefault();add(new FormData(e.currentTarget))}}><h2>Post an errand</h2><p>Market: {config.name} · {config.currency}</p><input name="title" required placeholder="What do you need done?"/><select name="category">{errandCategories.map(c=><option key={c.id}>{c.label}</option>)}</select><input name="location" required placeholder="Location"/><input name="price" required type="number" min="1" placeholder={`Budget in ${config.currency}`}/><div className="form-actions"><button type="button" className="secondary" onClick={()=>setPost(false)}>Cancel</button><button className="primary">Publish task</button></div></form></div>}{auth&&<Auth type={auth} close={()=>setAuth(null)} success={()=>{setAuth(null);setDash(true)}}/>}</div>}
function Auth({type,close,success}:{type:'signup'|'login';close:()=>void;success:()=>void}){return <div className="modal"><form className="auth" onSubmit={e=>{e.preventDefault();success()}}><button type="button" className="close" onClick={close}><X/></button><span className="auth-logo"><Sparkles/></span><h2>{type==='signup'?'Join ErrandGo':'Welcome back'}</h2><p>{type==='signup'?'Create your free account and start getting things done.':'Log in to manage your errands and earnings.'}</p>{type==='signup'&&<div className="twocol"><input required placeholder="First name"/><input required placeholder="Last name"/></div>}<input required type="email" placeholder="Email address"/><input required placeholder="Phone number"/>{type==='signup'&&<select defaultValue="NG">{countryConfigs.map(c=><option value={c.code} key={c.code}>{c.name}</option>)}</select>}<input required type="password" placeholder="Password"/><label className="check"><input type="checkbox" required/> I agree to the Terms and Privacy Policy</label><button className="primary full">{type==='signup'?'Create account':'Log in'} <ArrowRight/></button><div className="switch">{type==='signup'?'Already have an account?':'New to ErrandGo?'} <button type="button" onClick={success}>{type==='signup'?'Log in':'Sign up'}</button></div></form></div>}
function Dashboard({config,wallet,back}:{config:any;wallet:number;back:()=>void}){return <div className="dashboard"><header><div className="brand"><span className="logo"><Sparkles size={19}/></span><b>Errand<span>Go</span></b></div><button className="textbtn" onClick={back}>← Marketplace</button></header><div className="dash-wrap"><aside><div className="user"><div className="avatar big">CM</div><b>My ErrandGo</b><small>Verified member</small></div>{['Overview','My errands','My jobs','Wallet','Messages','Notifications','Profile','Settings'].map((x,i)=><button className={i===0?'selected':''} key={x}>{x}</button>)}</aside><main><div className="dash-head"><div><span className="eyebrow">DASHBOARD</span><h1>Good to see you 👋</h1><p>Manage your tasks, jobs and money in one place.</p></div><button className="primary">Post an errand <Plus/></button></div><div className="dash-cards"><div className="balance"><small>Available wallet balance</small><strong>{config.symbol}{wallet.toLocaleString()}</strong><button>+ Add money</button></div><div><small>Open errands</small><strong>4</strong><span>View requests <ArrowRight/></span></div><div><small>Jobs completed</small><strong>28</strong><span>Keep earning <ArrowRight/></span></div></div><div className="dash-grid"><section><div className="subhead"><h3>Recent activity</h3><button>View all</button></div>{['Groceries delivered','Office documents picked up','Apartment cleaning'].map((x,i)=><div className="activity" key={x}><span>{['🛒','📦','🏠'][i]}</span><div><b>{x}</b><small>{i+1} day{i?'s':''} ago · Completed</small></div><strong>+{config.symbol}{[8500,7000,30000][i].toLocaleString()}</strong></div>)}</section><section className="profile-card"><UserRound/><h3>Complete your profile</h3><p>Add your photo, skills and verification details to get better matches.</p><button className="secondary">Complete profile</button></section></div></main></div></div>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);